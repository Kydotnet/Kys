#!/bin/kys

kyl load "std"
kys cmdargs args

var i = 0;


for(; i < 1000; i++) trace(i);

for(var j = 0; j < 5; j += 2)
{
	trace(j);
}

var j = 0;

var uno = 5++;

uno ^= 2;

trace(uno);

var dos = uno++;

trace(dos);

trace(uno);
trace(dos);