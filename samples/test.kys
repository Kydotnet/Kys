#!/bin/kys

for(var a = 0; a!=10; 2)
{
	trace(a);
}

var int = type("System.Int32");

var array = tarr(int,47);

trace(array);

trace(typeof("Hello world"));

timed while(true,500)
{
	trace("hello");
}
timeout(2500)
{
	trace("demasiados hello!");
}

trace("ufas");