#!/bin/kys -t

kyl load "std"
kys cmdargs args

var intType = type("System.Double");
var int = tarr(intType, 0 ,1 , 2, 3, 4);

for(var i = 0; i < arrlen(int);i++)
{
	trace(getarr(int, i));
}

trace(int);