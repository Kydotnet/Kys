#!/home/koto/bin/kys

// the exit sentence is optional
// an exit 0 sentence is implicit at the of the program
// you can define multiple exit sentences but only he first detected define the exit code
exit 1;

// no executed
trace("hello world");